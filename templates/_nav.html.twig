<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<a class="navbar-brand" href="{{path('app_home')}}">Dealgames</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse" id="navbarNavAltMarkup">
		<div class="navbar-nav">

			{% if app.user == null %}
				<a class="nav-item nav-link" href="{{path('app_login')}}">Connexion</a>
				<a class="nav-item nav-link" href="{{path('app_register')}}">Register</a>
			{% else %}
				<a class="nav-item nav-link" href="{{path('app_home')}}">Home</a>
				<a class="nav-item nav-link" href="{{path('app_myaccount')}}">Mon Compte</a>
				<a class="nav-item nav-link" href="{{path('app_myannonce')}}">Mes annonces</a>
				<a class="nav-item nav-link" href="{{path('app_logout')}}">Déconnexion</a>
				<a class="nav-item nav-link" href="{{ path('app_annonce_new') }}">Nouvelle annonce</a>
			{% endif %}


		</div>

	</div>
</nav>


<div class="container">
	<div class="d-flex justify-content-center ">
		<a class="mx-4"href="#">pd</a>
		<a class="mx-4"href="#">pd</a>
		<a class="mx-4"href="#">pd</a>
	</div>
</div>
