{% extends 'base.html.twig' %}

{% block title %}Annonce
{% endblock %}

{% block body %}
		<div class="container"> <div class="row">
			<img src="{{ vich_uploader_asset(annonce) }}" alt="">
			<div>{{ annonce.annonceTitle }}</div>
			<div>{{ annonce.annonceDate ? annonce.annonceDate|date('Y-m-d H:i:s') : '' }}</div>
			<div>{{ annonce.user.nom  }}
				{{annonce.user.prenom}}</div>
			<div>Description :
				{{ annonce.annonceDescription }}</div>
			<div>contact :
				{{annonce.user.email}}
			</div>

		</div>
	</div>
	{# <pre>
	          {{ dump(annonce) }}
	        </pre> #}
	{# <a href="{{ path('app_home') }}">back to list</a> #}

	{# Condition pour vérifier si c'est l' user à le droit #}
		{% if is_granted("annonce_edit", annonce) %}
			<a class="btn btn-primary" href="{{ path('app_annonce_edit', {'id': annonce.id}) }}"> edit</a>
		{{ include('annonce/_delete_form.html.twig') }}

	{% endif %}

{% endblock %}
